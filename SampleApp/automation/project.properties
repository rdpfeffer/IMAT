# This is the same value as what is generated when running "generate-project"
# Although its current use is limited, if any we plan to use in in further 
# code generation commands which will allow for simple usage etc.
project.prefix=AUTO

